apiVersion: v1
kind: Namespace
metadata:
  name: aidp-governance
  labels:
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/audit: restricted
    pod-security.kubernetes.io/warn: restricted
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-data-governance
  namespace: aidp-governance
automountServiceAccountToken: true
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-data-governance-gateway
  namespace: aidp-governance
  labels:
    app.kubernetes.io/name: ai-data-governance-gateway
    app.kubernetes.io/version: "2.1.0"
spec:
  replicas: 2
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ai-data-governance-gateway
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ai-data-governance-gateway
    spec:
      serviceAccountName: ai-data-governance
      automountServiceAccountToken: true
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: gateway
          image: ${jsonencode(gateway_image)}
          imagePullPolicy: IfNotPresent
          args:
            - python
            - -m
            - governance_gateway.tls_entrypoint
          env:
            - name: GOVERNANCE_OCI_REGION
              value: ${jsonencode(oci_region)}
            - name: GOVERNANCE_OIDC_ISSUER
              value: ${jsonencode(oidc_issuer)}
            - name: GOVERNANCE_OIDC_AUTHORITY
              value: ${jsonencode(oidc_authority)}
            - name: GOVERNANCE_OIDC_AUDIENCE
              value: ${jsonencode(oidc_audience)}
            - name: GOVERNANCE_TLS_SECRET_OCID
              value: ${jsonencode(tls_secret_ocid)}
            - name: GOVERNANCE_AIDP_PLATFORM_ID
              value: ${jsonencode(aidp_platform_id)}
            - name: GOVERNANCE_JDBC_SECRET_OCID
              value: ${jsonencode(jdbc_secret_ocid)}
            - name: GOVERNANCE_JDBC_USER_OCID
              value: ${jsonencode(jdbc_user_ocid)}
            - name: GOVERNANCE_JDBC_DRIVER_OBJECT
              value: ${jsonencode(jdbc_driver_object)}
            - name: GOVERNANCE_JDBC_DRIVER_BUCKET
              value: ${jsonencode(jdbc_driver_bucket)}
            - name: GOVERNANCE_OBJECT_STORAGE_NAMESPACE
              value: ${jsonencode(object_storage_namespace)}
            - name: GOVERNANCE_TOKENIZATION_KEY_OCID
              value: ${jsonencode(tokenization_key_ocid)}
            - name: GOVERNANCE_TOKENIZATION_CRYPTO_ENDPOINT
              value: ${jsonencode(tokenization_crypto_endpoint)}
            - name: GOVERNANCE_CONTROL_CATALOG
              value: aidp_lab
            - name: GOVERNANCE_CONTROL_SCHEMA
              value: oci_control
            - name: GOVERNANCE_MAX_RESULT_ROWS
              value: "1000"
            - name: GOVERNANCE_MAX_RESULT_BYTES
              value: "5242880"
          ports:
            - name: https
              containerPort: 8443
              protocol: TCP
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 2Gi
          readinessProbe:
            httpGet:
              scheme: HTTPS
              path: /readyz
              port: https
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              scheme: HTTPS
              path: /healthz
              port: https
            periodSeconds: 30
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 10001
          volumeMounts:
            - name: runtime
              mountPath: /tmp
      volumes:
        - name: runtime
          emptyDir:
            sizeLimit: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: ai-data-governance-gateway
  namespace: aidp-governance
  annotations:
    oci.oraclecloud.com/load-balancer-type: "lb"
    service.beta.kubernetes.io/oci-load-balancer-internal: "true"
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: ai-data-governance-gateway
  ports:
    - name: https
      port: 8443
      targetPort: https
      protocol: TCP
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: ai-data-governance-gateway
  namespace: aidp-governance
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ai-data-governance-gateway
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: ai-data-governance-default-deny
  namespace: aidp-governance
spec:
  podSelector: {}
  policyTypes: ["Ingress", "Egress"]
  ingress:
    - ports:
        - protocol: TCP
          port: 8443
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - protocol: TCP
          port: 443
