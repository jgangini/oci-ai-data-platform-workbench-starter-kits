# Improvement Log - oci-aidp-cloud-migration-lab

Use this file for evidence-backed harness improvements in this repo.

Keep entries short. Record real friction, recurring overhead, or meaningful improvements only.

## Promotion Thresholds

- 2 recurrences in this repo -> local `.codex/AGENTS.md` candidate
- 3 recurrences or safety-critical repetition -> script or skill candidate
- Cross-repo or clearly universal pattern -> global `~/.codex/AGENTS.md` candidate

## Entry Template

| Date | Task or Incident | Friction Observed | Evidence | Action Taken or Proposed | Promotion Target | Status |
| --- | --- | --- | --- | --- | --- | --- |
| YYYY-MM-DD |  |  |  |  | local AGENTS / script / skill / global AGENTS / none | captured |
| 2026-07-03 | Initial AIDP lab | OCI Provider 8.21 exposes the Identity Domains App resource but no App Templates data source | `terraform validate` rejected `oci_identity_domains_app_templates`; provider schema confirmed it absent | Use the documented `CustomWebAppTemplateId` well-known identifier directly and keep a mock-provider contract test | local AGENTS | captured |
| 2026-07-03 | Initial AIDP lab | AIDP catalog resources are REST-only and their request shapes are easy to guess incorrectly | Official 20260430 Swagger distinguishes POST permissions from PUT volume permissions | Treat the published Swagger as the hook contract and cover canonical payloads with fakes | local AGENTS | captured |
| 2026-07-03 | Deploy Studio hook integration | Repository-relative hook paths and the worker's Python dependency boundary differ from the Terraform package root | Runner validation found a missing entrypoint and no top-level `requests` module | Test the manifest path from repo root and import the OCI SDK's vendored requests in hooks | local AGENTS | captured |
| 2026-07-03 | AIDP Quick Start alignment | Published preview examples and the live Workbench contract can use different endpoint generations | The operator profile returns `404` through `aidpprod.../20260430/aiDataPlatforms` but succeeds through the plugin-backed `datalake.../20240831/dataLakes` endpoint | Keep an exact live-validated endpoint assertion in hook and runtime tests | local AGENTS | resolved |
| 2026-07-03 | Final deployment audit | Serialized substring checks could combine role and permission values from different paginated rows | AIDP permission responses expose `grantee`, `granteeName`, `granteeType`, and `granteePermissions` per item | Keep same-item permission predicates and pagination tests at the REST boundary | local AGENTS | resolved |
| 2026-07-03 | Compute fallback audit | Shape listing proves compatibility, not launch capacity | OCI capacity reports expose `AVAILABLE` plus `available_count` for the requested OCPU/memory shape | Run a repository preflight capacity report and document the residual report-to-launch race | local AGENTS | resolved |
| 2026-07-07 | Live AIDP test2/test3/test4 | A closed Identity Domain JWK caused repeated OSCS failures, and a manual platform reused a required policy later deleted by another stack | Audit showed `Any Client` denied `SigningCert/jwk`; enabling public signing-certificate access fixed new OSCS setup, while restoring the 9 core statements recovered test4 Logging | Gate JWK access in preflight, keep required policy ownership stable, and separate optional network/object-deletion grants | local AGENTS | resolved |
| 2026-07-07 | Lab v2 participant reconciliation | Participant-writable storage cannot be the authority for immutable industry or exact cleanup | A participant with bucket object management could alter the public manifest before a retry or delete | Keep authoritative participant state in operator-admin-only `/Workspace/medallon/.control`; treat the visible manifest as descriptive | local AGENTS | resolved |
| 2026-07-07 | Lab v2 frontend validation | Regex-only source assertions did not exercise polling deadlines, `Retry-After`, aborts, or users behind shared NAT | Executable polling tests exposed behavior that static checks could not prove; an nginx per-IP limiter would throttle unrelated students | Keep the small pure polling helper and executable tests; rate-limit invalid codes by IP and valid reconciliations by opaque email key | local AGENTS | resolved |
| 2026-07-07 | Lab v2 architecture preflight | A second concurrent preflight overwrote the saved Sentrux baseline | The baseline timestamp and quality signal changed before postflight despite no intentional baseline reset | Make baseline capture single-owner or write-once per task and require explicit approval before restoring a user baseline | script | proposed |
| 2026-07-07 | Release v1.0.0 credential cleanup | Nested Python bytecode entered the Docker context and retained obsolete OAuth strings | The final image scan found `app/__pycache__/*.pyc` even though top-level cache names were ignored | Exclude nested `__pycache__`, `*.py[cod]`, and pytest caches; keep the image credential/cache scan in release validation | none | resolved |
| 2026-07-07 | Release v1.0.0 architecture gate | Sentrux printed `DEGRADED` while returning exit code 0, so the wrapper continued | First postflight reported complex functions `2 -> 3` but still printed completion | Make `arch-postflight.ps1` fail on either nonzero exit or textual `DEGRADED` | script | resolved |
| 2026-07-08 | Lab-6 operator identity simplification | A dedicated technical user duplicated the uploaded administrator identity and failed during Identity Domains creation | PLAN and AIDP creation succeeded, but APPLY stopped on `oci_identity_domains_user.provisioner`; the uploaded operator already passed a signed Identity Domains query | Remove provisioner identity/RBAC and deliver the exact operator profile once using RSA-OAEP/SHA-256 plus AES-256-GCM, exact-object instance-principal access, and verified deletion | local AGENTS | resolved |
| 2026-07-08 | Operator credential bootstrap retry | Deleting the one-use RSA key made a successful bootstrap impossible to reconcile on a repeated post-apply hook | A second hook run would request another public key after the VM had already removed it | Return a public-only `READY` marker, verify installed config/key on object 404, prevent object overwrite, and keep strict health as the final acknowledgement | local AGENTS | resolved |
| 2026-07-08 | Lab-7 Workbench authentication correction | The first probe falsely attributed `404 NotAuthorizedOrNotFound` to API-key identity instead of endpoint drift | With the same `config` and `key.pem`, `codex-oci` and raw signed requests returned `200` for `/roles`, `/workspaces`, and `/catalogs` through `datalake.../20240831/dataLakes`; the operator is a direct `AI_DATA_PLATFORM_ADMIN` member | Reuse the uploaded operator profile, pin the validated Workbench endpoint, and retain the signed `/roles` probe as a readiness check | local AGENTS | resolved |
| 2026-07-08 | Lab-7 live registration | The live 20240831 API differs from generated assumptions in several small but blocking response contracts | Notebook-content GET remains 500 after PUT 200 while export succeeds, jobs add nullable dependency fields, and job-permission lists fail when `limit` is omitted | Verify notebooks through export, normalize server-added fields, send an explicit list limit, and retain the four-industry OCI-local refresh matrix | local AGENTS | resolved |
| 2026-07-08 | Lab-7 collaborative layout | Per-user schemas make the catalog unusable at classroom scale, while email-named folders are needed for navigation | One failed participant already created four schemas; 50 participants would create 200 schemas | Use four shared `oci_*` schemas, exact opaque-key table names, `/Workspace/medallon/<email>`, and manifest-scoped cleanup that never deletes shared schemas | local AGENTS | resolved |
| 2026-07-08 | OCI-local readiness flapping | Browser and Docker probes each performed five live OCI calls with no cache | Rapid polling produced repeated `503` responses; probes recovered after polling stopped | Single-flight the deep readiness probe and cache success for 30 seconds and failure for 5 seconds | local AGENTS | resolved |
| 2026-07-13 | Access Signing Certificate preflight | A preflight blocked deployments on a tenant-wide JWK setting that the lab does not consume | With `signingCertPublicAccess=false`, the deployed lab health check passed and a fresh STRUCTURED AIDP reached ACTIVE | Remove the JWK preflight and treat the setting as tenant policy | local AGENTS | resolved |
| 2026-07-08 | Administrator participant reset | A naive cleanup-then-provision retry can delete the newly rebuilt environment when the prior success response is lost | The reset endpoint is polled across eventually consistent AIDP deletes and creates | Journal a browser-generated operation UUID through `cleanup`, `provision`, and `complete` in the operator-only participant control manifest | local AGENTS | resolved |
| 2026-07-08 | Linux release test imports | Calling the `pytest` executable did not place the repository root on `sys.path` in GitHub Actions | Release v1.0.1 failed collection with `ModuleNotFoundError: scripts` although the Windows command used an explicit `PYTHONPATH` | Invoke tests as `python -m pytest` in both CI workflows so the checkout root is imported consistently | workflow | resolved |
| 2026-08-12 | Reproducible multi-lab packages | Participant-specific generators coupled every new notebook and dataset to Python release logic | Banking, Telecommunications, Retail, and Healthcare required the same deterministic source bytes while only paths, parameters, jobs, tables, and permissions varied | Check in hashed package manifests and canonical assets; derive arbitrary workflow tasks from each manifest | local AGENTS | resolved |
| 2026-08-12 | Architecture postflight contract | The wrappers skipped `sentrux check .` whenever `.sentrux/rules.toml` was absent even though the repo gate requires the default check signal | Preflight saved a gate baseline but did not execute the requested check on this repository | Run `sentrux check .` whenever Sentrux is installed; continue treating a missing rules file as no custom rules | script | resolved |
| 2026-08-12 | Live v1.0.0 baseline deployment | A fresh STRUCTURED AIDP remained in `CREATING` beyond the provider request deadline and failed after 59 minutes | Resource Manager ended with `now() + computed backoff duration exceeds request deadline`; exact-stack destroy later removed 17 resources successfully | Set the AIDP resource create timeout to 120 minutes and retain a Terraform contract assertion before publishing a replacement RC | local AGENTS | resolved |
| 2026-08-13 | Live RC2 participant registration | AIDP rejected a participant workspace folder containing email characters after infrastructure and post-apply had succeeded | The protected v3 manifest was written, but all four lab journals remained at `workspace` and the create-folder call returned HTTP 400 | Use the existing opaque `participant_key` as the v3 workspace folder, preserve active v2 paths, and require a live registration with a plus-address alias before final release | local AGENTS | resolved |
| 2026-08-13 | Live RC3 participant content | A generic AIDP `400` hid whether the rejected operation was schema, file, notebook, or job provisioning | Keyed workspace creation succeeded, but the Banking journal remained at the pre-content `schemas` checkpoint and retries returned the same generic error | Surface only redacted method, endpoint, phase, workspace path, and Oracle `code`/`message`; never include request bodies, auth headers, PII, OCIDs, or PAR URLs | local AGENTS | resolved |
| 2026-08-13 | Live RC4 job reconciliation | The task-parameter fixture used the plausible but invalid field `key` and only compared generated data to itself | AIDP returned `InvalidParameter` with `tasks[*].parameters[*].name must not be null`; the published `Parameter` schema also requires `name` | Assert the external request field explicitly as `name` in the arbitrary-notebook job contract test and retain the live diagnostic boundary | local AGENTS | resolved |
| 2026-08-13 | Native Iceberg lineage probe | Enabling lineage and restarting compute did not make native external Iceberg writes expose the complete lineage contract | Writer V2 returned no entity or column links; explicit Iceberg Writer V1 and SQL produced entity links but still no column links, while an existing Delta-backed table returned both | Keep the native Iceberg and Writer V2 contract, stop before participant assignment, and require a live-supported write path or Oracle runtime correction before acceptance | local AGENTS | captured |
| 2026-08-14 | Administrative lab assignment timeout | nginx closed a valid add-lab request at its 60-second default while the backend later completed the checkpoint with `202`, causing the UI to display a false `504` failure | The proxy logged `upstream timed out`; the manifest advanced to `permissions` and the 14-task job existed without its final `MANAGE` grant | Give API proxy reads/writes five minutes and make polling retry `408`/`502`/`504` plus transient fetch failures while preserving application `503` errors | local AGENTS | resolved |
| 2026-08-14 | Telco full medallion lineage | Direct Silver-to-Gold edge assertions passed even though external source tables were represented by synthetic path-derived nodes and hid the governed Landing/Bronze/Silver schemas | Live `fetchLineage`, export, and Master Catalog DOM at depth 8 showed external tables as `aidp_lab.telco_lineage.*`; managed catalog tables restored 10 Landing, 10 Bronze, 7 Gold-reachable Silver, and 3 Gold nodes with zero synthetic nodes | Require qualified schema coverage and a forbidden synthetic-prefix assertion across all Gold anchors, in addition to direct edge checks | local AGENTS | resolved |
| 2026-08-17 | Governance Agent lineage evaluation | AIDP accepted a table anchor for `level=COLUMN` but returned `404 Unknown resource` for a column-qualified anchor; raw process nodes also omitted the notebook path needed by learners | The same live table returned 85 nodes and 73 links from `fetchLineage`, while the column URI returned 404; the job endpoint mapped task keys to notebook paths and dependencies | Anchor column requests on the table, select the requested column's connected component client-side, enrich process tasks from the job endpoint, and retain an executable graph fixture | local AGENTS | resolved |
| 2026-08-22 | Governance gateway OKE deployment | The first DevOps server-side apply started immediately after creating its exact dynamic-group policy and failed without project service logs; Kubernetes readiness also depended on a JDBC driver intentionally uploaded after installation | Resource Manager reached 95%, OKE was active, and the failure occurred in `Apply kubernetes manifest` before rollout status | Add a one-time bounded IAM propagation gate, enable 30-day DevOps project logs, use `/healthz` for pod readiness, and retain `/readyz` for JDBC/control-plane readiness | local AGENTS | resolved locally; live gate pending |
| 2026-08-23 | Parallel OCI-local previews | A fixed Compose name, environment path, and port made one local profile overwrite or block another deployment. | Isolated Deploy Studio deployments need to remain testable concurrently and retain their own access credentials. | Parameterize the Compose project, environment path, and loopback port; keep the private key outside the generated profile. | script | applied |
| 2026-08-24 | New Vault DNS propagation | One successful DNS lookup was not enough to prove that a new regional KMS endpoint was stable. | The wait gate passed once, then `oci_kms_key` failed four minutes later with `no such host`. | Require six consecutive successful lookups before creating the governance key and keep a regression assertion. | local AGENTS | resolved |
| 2026-08-24 | Governance extension v2.2.0 | Phase-heavy singleton lifecycle functions crossed the architecture complexity threshold even though their behavior tests passed. | Final Sentrux gate reported complex functions `2 -> 7` before release. | Split compute recovery, synchronization checks, install/redeploy, delete, and identity resolution into phase helpers while retaining focused regression tests. | none | resolved |
